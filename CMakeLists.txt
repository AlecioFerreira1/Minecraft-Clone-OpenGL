# cmake_minimum_required(VERSION 3.10)
# project(MCClone)

# set(CMAKE_CXX_STANDARD 17)

# find_package(OpenGL REQUIRED)

# include_directories(include)
# link_directories(C:/caminho/para/glfw/lib)

# add_executable(mcclone src/main.cpp src/glad.c)

# target_link_libraries(mcclone OpenGL::GL glfw3)